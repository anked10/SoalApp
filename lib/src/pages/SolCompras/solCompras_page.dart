import 'package:flutter/material.dart';


class SolComprasPage extends StatelessWidget {
  const SolComprasPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}